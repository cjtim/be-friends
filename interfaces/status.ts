/* eslint-disable no-unused-vars */
// eslint-disable-next-line no-shadow
export enum Status {
  NEW = 'new',
  PENDING = 'pending',
  ADOPED = 'adoped',
}

export const statues = [Status.NEW, Status.PENDING, Status.ADOPED]
